pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
    includeBuild('../../node_modules/@react-native/gradle-plugin')
}

plugins {
    id("com.facebook.react.settings")
}

rootProject.name = 'truvideoreactvideosdk.example'

include ':app'

// include ':truvideo-react-video-sdk'
// project(':truvideo-react-video-sdk').projectDir = new File(rootProject.projectDir, '../../../android')

extensions.configure(com.facebook.react.ReactSettingsExtension) { ex ->
    ex.autolinkLibrariesFromCommand()
}
